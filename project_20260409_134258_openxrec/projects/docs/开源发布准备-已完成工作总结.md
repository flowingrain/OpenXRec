# 开源发布准备 - 已完成工作总结

## 📅 执行时间

2026-03-30

## ✅ 已完成的工作

### 1. 开源发布准备指南

**文件**：`docs/开源发布准备指南.md`

**内容**：
- 🔴 必须移除的内容（安全敏感）
  - 环境变量文件
  - 用户数据
- 🟡 必须修改的内容（替换为占位符）
  - 域名和 URL
  - 模型名称配置（新增）
  - 文档中的示例配置
- 🟠 建议移除的内容（清理项目）
  - 临时和测试文件
  - 内部文档
  - 未使用的文档
- 🟢 需要保留并修改的内容
  - .gitignore 文件
  - 开源配置文件
- 📋 开源前检查清单
- 🔧 开源后的配置说明

### 2. .gitignore 文件更新

**文件**：`.gitignore`

**变更**：
```gitignore
# 新增
# User data
data/
*.json
```

### 3. .env.example 文件创建

**文件**：`.env.example`

**内容**：
- Supabase 配置占位符
- 大语言模型配置说明
- 域名配置（可选）
- Coze SDK 配置（可选）
- 对象存储配置（可选）
- 日志配置（可选）

### 4. README.md 文件更新

**文件**：`README.md`

**变更**：
- 替换域名为占位符：`https://your-domain.com`
- 更新模型名称为环境变量配置：`支持 GPT-4、Claude-3、Doubao 等（通过环境变量配置）`
- 更新数据库描述：`Supabase (PostgreSQL + pgvector)`
- 添加"配置环境变量"章节
- 添加开源相关链接和说明
- 添加贡献指南和安全政策链接

### 5. CONTRIBUTING.md 文件创建

**文件**：`CONTRIBUTING.md`

**内容**：
- 行为准则
- 开发环境搭建
- 代码规范
- 提交信息规范
- Bug 报告模板
- 功能请求模板
- Pull Request 流程
- 测试指南
- 文档编写规范
- 发布流程

### 6. SECURITY.md 文件创建

**文件**：`SECURITY.md`

**内容**：
- 支持的版本
- 漏洞报告流程
- 安全最佳实践
- 安全头部配置
- 漏洞公告渠道
- 已知漏洞记录
- 安全团队信息
- 相关资源

### 7. LICENSE 文件创建

**文件**：`LICENSE`

**内容**：
- MIT License 完整文本
- 版权声明：智弈团队 (Zhiyi Team)

---

## ⚠️ 剩余需要执行的操作（开源前）

### 高优先级（必须执行）

#### 1. 删除敏感信息

```bash
# 删除 .env.local 文件
rm .env.local

# 删除 data/ 目录及所有用户数据
rm -rf data/

# 删除 .env.local 如果存在
if [ -f .env.local ]; then rm .env.local; fi
```

#### 2. 替换所有硬编码的模型名称

**影响文件**：约 80+ 个文件

**需要替换的内容**：
```typescript
// ❌ 修改前
model: 'doubao-seed-2-0-pro-260215'

// ✅ 修改后
model: process.env.LLM_MODEL || 'gpt-4'
```

**执行脚本**：
```typescript
// 创建 scripts/replace-model-names.ts
import { glob } from 'glob';
import { readFileSync, writeFileSync } from 'fs';

const files = await glob('src/**/*.{ts,tsx}');

files.forEach(file => {
  const content = readFileSync(file, 'utf-8');
  const updated = content
    .replace(
      /model:\s*['"]doubao-seed-2-0-pro-260215['"]/g,
      "model: process.env.LLM_MODEL || 'gpt-4'"
    )
    .replace(
      /model:\s*config\.model\s*\|\|\s*['"]doubao-seed-2-0-pro-260215['"]/g,
      "model: config.model || process.env.LLM_MODEL || 'gpt-4'"
    );

  if (updated !== content) {
    writeFileSync(file, updated);
    console.log(`✅ Updated: ${file}`);
  }
});

console.log(`\n🎉 Completed! Updated files: ${files.length}`);
```

**运行脚本**：
```bash
cd scripts
npx ts-node replace-model-names.ts
```

**验证替换**：
```bash
# 检查是否还有硬编码的模型名称
grep -r "doubao-seed-2-0-pro-260215" src/
```

#### 3. 替换文档中的域名

**需要更新的文件**：
- `PROJECT_OVERVIEW.md`
- `docs/项目完成度分析.md`
- `docs/开源发布准备指南.md`（示例）

**执行替换**：
```bash
# 替换所有文档中的域名
find docs -name "*.md" -type f -exec sed -i 's|https://abc123.dev.coze.site|https://your-domain.com|g' {} \;
```

### 中优先级（建议执行）

#### 4. 删除内部文档

```bash
# 删除项目提案
rm docs/project-proposal-2026-03-30.md

# 删除 Python 后端相关文档（已移除后端）
rm docs/Python后端与Coze SDK兼容性分析.md
rm docs/前后端集成指南.md

# 删除或简化源代码文档（过于详细）
# 可选择删除或精简
# rm docs/源代码文档.md

# 删除其他内部文档
# rm docs/dianxin-plan.md
```

#### 5. 清理临时文件

```bash
# 删除测试脚本
rm scripts/test-memory-system.ts

# 删除临时构建产物
rm -rf .next/
rm -rf node_modules/.cache/
```

### 低优先级（可选执行）

#### 6. 优化文档结构

- 整理 docs/ 目录结构
- 合并重复的文档
- 更新过时的内容

#### 7. 添加示例数据

- 创建示例分析案例
- 提供演示数据
- 编写快速开始教程

---

## 📋 开源前检查清单

使用以下检查清单确保所有步骤都已执行：

### 安全检查

- [ ] 删除 `.env.local` 文件
- [ ] 删除 `data/` 目录及所有用户数据
- [ ] 检查并删除所有硬编码的密钥或凭证
- [ ] 确保 `.gitignore` 包含所有敏感文件

### 内容检查

- [ ] 替换所有域名为占位符
- [ ] 替换所有硬编码的模型名称为环境变量（约 80+ 个文件）
- [ ] 删除内部文档和临时文件
- [ ] 确保所有示例使用占位符
- [ ] 更新 README 文件

### 开源文件

- [x] 创建 LICENSE 文件
- [x] 创建 CONTRIBUTING.md 文件
- [x] 创建 SECURITY.md 文件
- [x] 创建 .env.example 文件
- [x] 更新 .gitignore 文件
- [x] 创建开源发布准备指南

### 代码检查

- [ ] 运行 `pnpm run lint` 确保代码规范
- [ ] 运行 `pnpm run build` 确保构建成功
- [ ] 运行 `npx tsc --noEmit` 确保类型检查通过
- [ ] 测试所有核心功能

---

## 🔧 开源后配置说明

### 用户需要自行配置的内容

1. **Supabase 配置**：
   - 创建 Supabase 项目
   - 执行数据库迁移脚本
   - 配置环境变量

2. **环境变量**：
   ```bash
   NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key
   SUPABASE_DB_URL=postgresql://postgres:password@db.your-project.supabase.co:5432/postgres
   LLM_MODEL=gpt-4
   ```

3. **数据库迁移**：
   ```bash
   psql -h db.your-project.supabase.co -U postgres -d postgres -f supabase/init.sql
   ```

---

## 📝 注意事项

1. **不要立即执行删除操作**
   - 删除 `.env.local` 和 `data/` 会影响当前运行环境
   - 建议在完全准备好开源后再执行这些操作

2. **模型名称替换的影响**
   - 替换模型名称后，需要确保环境变量 `LLM_MODEL` 已配置
   - 建议先在测试环境验证

3. **备份重要数据**
   - 在删除任何文件前，确保已备份重要数据
   - 特别是 `data/` 目录中的用户数据

4. **验证代码**
   - 替换模型名称后，运行完整的测试套件
   - 确保所有功能正常工作

---

## 📚 参考文档

- [开源发布准备指南](./docs/开源发布准备指南.md)
- [贡献指南](./CONTRIBUTING.md)
- [安全策略](./SECURITY.md)
- [.env.example](./.env.example)

---

## 🎯 下一步行动

1. **当前阶段**：已完成准备工作文档和模板
2. **下一阶段**：选择合适的时机执行剩余操作
3. **最终阶段**：推送到 GitHub/GitLab，正式开源

---

**创建日期**：2026-03-30
**最后更新**：2026-03-30
